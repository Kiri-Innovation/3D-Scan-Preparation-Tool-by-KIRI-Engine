# PyTorch is pinned separately in requirements-torch-cu128.txt so the
# installer build uses the CUDA 12.8 wheels instead of accidentally
# freezing an older or CPU-only local torch install.

# Core Image & Video Processing
numpy==1.26.4
opencv-python>=4.9.0
imageio-ffmpeg>=0.5.1
Pillow>=10.2.0
pillow-heif>=0.18.0
exifread==3.0.0
rawpy==0.19.0
scenedetect==0.6.3
scipy>=1.11.0

# AI Models (YOLO & MaskFormer)
ultralytics>=8.1.24
transformers>=4.38.2
safetensors>=0.4.0

# BiRefNet Architecture Dependencies
timm>=0.9.0
einops>=0.7.0
kornia>=0.7.1
